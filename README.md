To start:

- for every `.env.example` file, create a corresponding `.env` file in the same location and fill in any necessary values
- this repo only contains the stack files for Dockge and .env files, no data
- run `docker-compose up -d` in root to get Dockge up
- in Dockge UI, run the stacks
